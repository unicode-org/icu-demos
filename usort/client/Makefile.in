## Makefile.in for ICU - example/usort/client
## Stephen F. Booth
## Steven  R. Loomis

# This makefile.in is a little different, it's one of the first
# couple which can be run outside of the ICU build system (that is,
# without ICU having prior knowledge).  It MUST live in a certain
# directory relative to the ICU. 
#
# To create the Makefile, just do 'cd .. ; make'


## Shell to use
SHELL = @SHELL@

## Install directory information
srcdir = @srcdir@

# Can't get top_srcdir to work automatically, yet
top_srcdir = ../../../source
#top_srcdir = @top_srcdir@/source
prefix = @prefix@
exec_prefix = @exec_prefix@

bindir = @bindir@
sbindir = @sbindir@
datadir = @datadir@
libdir = @libdir@
includedir = @includedir@

pkgdatadir = $(datadir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@

## Install program information
mkinstalldirs = $(SHELL) $(top_srcdir)/mkinstalldirs

INSTALL = @INSTALL@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_DATA = @INSTALL_DATA@

## Compiler/tools information
CC = @CC@
CXX = @CXX@
PACKAGE = @PACKAGE@
VERSION = @VERSION@

## Platform-specific setup
@host_frag@

## Build directory information
top_builddir = $(top_srcdir)
exdir = example/usort/client
subdir = ../$(exdir)

## Extra files to remove for 'make clean'
CLEANFILES = *~

## Target information
TARGET = usort

DEFS = @DEFS@ -DCOMPOSE
CPPFLAGS = @CPPFLAGS@ -I$(top_srcdir)/common -I$(top_srcdir)/i18n -I../lib
CFLAGS = @CFLAGS@
CXXFLAGS = @CXXFLAGS@
LDFLAGS = @LDFLAGS@ \
$(LD_RPATH)$(LD_RPATH_PRE)$(libdir)@ld_rpath_suf@$(LD_RPATH_PRE)$(top_builddir)/common@ld_rpath_suf@$(LD_RPATH_PRE)$(top_builddir)/i18n@ld_rpath_suf@$(LD_RPATH_PRE)$(top_builddir)/../example/usort/lib
LIBS = @LIBS@ -L$(libdir) -L$(top_builddir)/common -licu-uc \
		-L$(top_builddir)/i18n -licu-i18n -L../lib   -lusort

OBJECTS = usort_main.o 

DEPS = $(OBJECTS:.o=.d)

## List of phony targets
.PHONY : all all-local install install-local clean clean-local \
distclean distclean-local dist dist-local check check-local

## Clear suffix list
.SUFFIXES :

## List of standard targets
all: all-local
install: install-local
clean: clean-local
distclean : distclean-local
dist: dist-local
check: check-local

all-local: $(TARGET)

install-local: all-local install-target

install-target:
	$(mkinstalldirs) $(bindir)
	$(INSTALL) $(TARGET) $(bindir)/$(TARGET)

dist-local:

clean-local:
	test -z "$(CLEANFILES)" || rm -f $(CLEANFILES)
	rm -f $(OBJECTS) $(TARGET)

distclean-local: clean-local
	rm -f Makefile $(DEPS)

check-local:

Makefile: $(srcdir)/Makefile.in  $(top_builddir)/config.status
	cd $(top_srcdir) \
	 && CONFIG_FILES=../$(exdir)/$@ CONFIG_HEADERS= $(SHELL) config.status

$(TARGET) : $(OBJECTS)
	$(LINK.cc) -o $@ $^ $(LIBS)

ifneq ($(MAKECMDGOALS),distclean)
-include $(DEPS)
endif



